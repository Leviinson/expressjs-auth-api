const path = require("path");

module.exports = {
    config: path.resolve("src", "db", "config.ts"),
    // original models are located in src/db/models/
    "models-path": path.resolve("dist", "db", "models"),
    "seeders-path": path.resolve("src", "db", "seeds"),
    "migrations-path": path.resolve("src", "db", "migrations"),
};
